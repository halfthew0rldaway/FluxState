---
/**
 * Warnings, Explanations, and Context Help Panel - Comprehensive
 */
---

<div class="warnings-layout">
    <!-- Row 1: Diagnostics (Full Width for readability) -->
    <div class="panel diagnostics-panel">
        <div class="panel-header">
            <span class="panel-title">System Diagnostics</span>
        </div>
        <div class="panel-content">
            <div id="warnings-container">
                <div class="no-warnings">System operating within normal parameters</div>
            </div>
        </div>
    </div>
    
    <!-- Row 2: Explanations & Details -->
    <div class="info-grid">
        <div class="explanations-column">
            <div class="panel explanation-panel">
                <div class="panel-header">
                    <span class="panel-title">Workload Analysis</span>
                </div>
                <div class="panel-content">
                    <p id="workload-explanation" class="explanation-text">
                        Select a workload to see detailed behavior explanation.
                    </p>
                </div>
            </div>
            
            <div class="panel explanation-panel">
                <div class="panel-header">
                    <span class="panel-title">Thermal Analysis</span>
                </div>
                <div class="panel-content">
                    <p id="thermal-explanation" class="explanation-text">
                        Thermal behavior will be analyzed as the simulation runs.
                    </p>
                </div>
            </div>
        </div>
        
        <div class="info-column">
            <div class="panel" id="details-panel">
                <div class="panel-header">
                    <span class="panel-title">Component Details</span>
                    <div class="panel-controls">
                        <div class="tab-group">
                            <button class="tab-btn active" data-tab="cpu">CPU</button>
                            <button class="tab-btn" data-tab="gpu">GPU</button>
                            <button class="tab-btn" data-tab="cooling">Cooling</button>
                        </div>
                        <button class="panel-toggle" id="details-toggle" title="Toggle Panel">
                            <i class="ph-bold ph-caret-up"></i>
                        </button>
                    </div>
                </div>
                <div class="panel-content collapsible-content">
                    <div id="component-details" class="component-details">
                        <!-- Content rendered by JS -->
                    </div>
                </div>
            </div>
            
            <div class="panel help-panel collapsed" id="help-panel">
                <div class="panel-header">
                    <span class="panel-title">Context Guide</span>
                    <button class="panel-toggle collapsed" id="help-toggle" title="Toggle Panel">
                        <i class="ph-bold ph-caret-up"></i>
                    </button>
                </div>
                <div class="panel-content collapsible-content" id="help-content">
                    <div id="context-help" class="context-help">
                        <div class="help-title">Hover for Help</div>
                        <div class="help-content">Hover over any configuration option to learn how it affects power consumption and thermal behavior. Each parameter in this simulator has a real effect on the calculated results.</div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    /* ... (previous styles) ... */

    /* Terminal Log Style for Diagnostics */
    .diagnostics-panel {
        background-color: #0c0c0e; /* Darker terminal bg */
        border: 1px solid var(--border-default);
    }
    
    .diagnostics-panel .panel-content {
        padding: 0;
        background-color: #000000;
    }
    
    #warnings-container {
        font-family: 'JetBrains Mono', monospace;
        font-size: 0.75rem;
        padding: var(--space-3);
        height: 200px;
        overflow-y: auto;
        display: flex;
        flex-direction: column;
        gap: 6px;
    }

    /* Scrollbar for terminal */
    #warnings-container::-webkit-scrollbar { width: 6px; }
    #warnings-container::-webkit-scrollbar-track { background: #111; }
    #warnings-container::-webkit-scrollbar-thumb { background: #333; border-radius: 3px; }

    .panel-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .panel-controls {
        display: flex;
        align-items: center;
        gap: var(--space-2);
    }

    .tab-group {
        display: flex;
        background: var(--bg-elevated);
        padding: 2px;
        border-radius: var(--radius-sm);
        border: 1px solid var(--border-subtle);
    }

    .tab-btn {
        background: transparent;
        border: none;
        color: var(--text-tertiary);
        padding: 4px 8px;
        font-family: var(--font-mono);
        font-size: 0.7rem;
        cursor: pointer;
        border-radius: 4px;
        transition: all 0.2s;
    }

    .tab-btn:hover { color: var(--text-secondary); }
    .tab-btn.active {
        background: var(--bg-tertiary);
        color: var(--text-primary);
        box-shadow: 0 1px 2px rgba(0,0,0,0.2);
    }

    .panel-toggle {
        background: transparent;
        border: 1px solid transparent;
        color: var(--text-tertiary);
        cursor: pointer;
        width: 24px;
        height: 24px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 50%;
        transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
    }
    
    .panel-toggle:hover {
        background: var(--bg-primary);
        color: var(--text-primary);
        border-color: var(--border-subtle);
    }
    
    .panel-toggle.collapsed {
        transform: rotate(180deg);
        background: var(--bg-tertiary);
        color: var(--text-primary);
    }

    /* Using global collapse logic from main.css for robustness */
    .warnings-explanations-grid {
        display: grid;
        grid-template-columns: 1fr 1fr 1fr;
        gap: var(--space-4);
    }
    
    @media (max-width: 1200px) {
        .warnings-explanations-grid {
            grid-template-columns: 1fr 1fr;
        }
        
        .info-column {
            grid-column: span 2;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: var(--space-4);
        }
    }
    
    @media (max-width: 800px) {
        .warnings-explanations-grid {
            grid-template-columns: 1fr;
        }
        
        .info-column {
            grid-column: span 1;
            grid-template-columns: 1fr;
        }
    }
    
    .diagnostics-panel {
        min-height: 200px;
    }
    
    .explanations-column {
        display: flex;
        flex-direction: column;
        gap: var(--space-3);
    }
    
    .info-column {
        display: flex;
        flex-direction: column;
        gap: var(--space-3);
    }
    
    .explanation-text {
        font-size: var(--font-size-sm);
        color: var(--text-secondary);
        line-height: 1.7;
        padding: var(--space-2);
        background-color: var(--bg-tertiary);
        border-radius: var(--radius-sm);
        border-left: 2px solid var(--border-emphasis);
        min-height: 4em;
    }
    
    .context-help {
        font-family: var(--font-mono);
        font-size: var(--font-size-xs);
    }
    
    .help-title {
        font-weight: 600;
        color: var(--text-primary);
        margin-bottom: var(--space-2);
        padding-bottom: var(--space-1);
        border-bottom: 1px solid var(--border-subtle);
    }
    
    .help-content {
        color: var(--text-secondary);
        line-height: 1.6;
        white-space: pre-wrap;
    }
    
    .component-details {
        font-family: var(--font-mono);
        font-size: var(--font-size-sm);
    }
    
    .detail-placeholder {
        color: var(--text-tertiary);
        text-align: center;
        padding: var(--space-4);
    }
    
    .detail-section {
        background: var(--bg-tertiary);
        border-radius: var(--radius-sm);
        padding: var(--space-3);
    }
    
    .detail-header {
        font-weight: 600;
        color: var(--text-primary);
        margin-bottom: var(--space-2);
        padding-bottom: var(--space-1);
        border-bottom: 1px solid var(--border-subtle);
    }
    
    .detail-row {
        display: flex;
        justify-content: space-between;
        padding: 4px 0;
        font-size: var(--font-size-xs);
    }
    
    .detail-label {
        color: var(--text-tertiary);
    }
    
    .detail-value {
        color: var(--text-primary);
    }
    
    .detail-value.boost {
        color: var(--color-boost);
    }
    
    .detail-description {
        font-size: var(--font-size-xs);
        color: var(--text-secondary);
        margin-top: var(--space-2);
        padding-top: var(--space-2);
        border-top: 1px solid var(--border-subtle);
        line-height: 1.5;
    }
</style>
